version: '3.8'

services:
  frontend:
    build:
      context: ./client/scrum-point
      dockerfile: Dockerfile
    ports:
      - "5173:80"  
    depends_on:
      - backend
    

  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
